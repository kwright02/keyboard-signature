replicaCount: 1

image:
  repository: ghcr.io/kwright02/keyboard-signature
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

resources: {}

ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: kwright02.dev
      paths:
        - path: /keyboard-signature
          pathType: ImplementationSpecific
  tls: []
