import type { NextConfig } from "next";


const nextConfig: NextConfig = {
  /* config options here */
  basePath: '/keyboard-signature',
  reactStrictMode: true,
  devIndicators: false
};

export default nextConfig;
